// todo: probably need extra info like "target velocity"
function waypointInit(xPos, yPos, angleVal, widthVal, radiusVal) {
    var result = {position : Vec2Init(xPos, yPos), angle : angleVal, width : widthVal, radius : radiusVal};
    return result;
}

function drawWaypoint(waypoint) {

}